/** AI 适配层配置常量 */

/** 流式总结合并 max tokens（避免过长输出占据浮窗空间） */
export const SUMMARY_MAX_TOKENS = 512;

/** 连接测试用最小 tokens（仅验证连通性） */
export const CONNECTION_TEST_MAX_TOKENS = 8;

/** Gemini 协议 temperature 默认值 */
export const GEMINI_DEFAULT_TEMPERATURE = 0.7;
